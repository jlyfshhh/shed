services:
  shed:
    build: .
    container_name: shed
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${SHED_PORT:-3000}:3000"
    volumes:
      - ${SHED_DATA_PATH:-./data}:/data
