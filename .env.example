# Required for the voice webhook. Never commit the real values.
ANTHROPIC_API_KEY=replace-with-your-anthropic-api-key
SHED_VOICE_TOKEN=replace-with-a-long-random-shared-secret

# Shed uses this zone when resolving "today" in voice requests.
SHED_TIME_ZONE=America/New_York

# Host port used by the Docker container.
PORT=3000
